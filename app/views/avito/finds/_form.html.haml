= form_for @avito_find do |f|
  - if @avito_find.errors.any?
    #error_explanation
      %h2= "#{pluralize(@avito_find.errors.count, "error")} prohibited this avito_find from being saved:"
      %ul
        - @avito_find.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :req
    = f.text_field :req
  .field
    = f.label :res
    = f.text_field :res
  .actions
    = f.submit 'Save'
